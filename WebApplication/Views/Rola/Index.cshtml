@model IEnumerable<WebApplication.Models.Rola>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nazwa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazwa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_roli">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_roli">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_roli">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
